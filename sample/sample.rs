
fn main {
    let stuff = b'H';  // Binary character.
    let stuff = b"Hello,";  // Binary string.
    let stuff = br###"Hello, a double quote (") is in this string."###;
    let stuff = r###"Hello, a dzouble quote (") is in this string."###;
}